export enum AxisValue {
  UP = "up",
  DOWN = "down",
  LEFT = "left",
  RIGHT = "right",
  DEFAULT = "center",
}

export enum GamepadButton {
  DOWN = 13,
  LEFT = 14,
  RIGHT = 15,
  UP = 12,
  ENTER = 0,
  ESCAPE = 1,
}
